import { PipedatethaiPipe } from './pipedatethai.pipe';

describe('PipedatethaiPipe', () => {
  it('create an instance', () => {
    const pipe = new PipedatethaiPipe();
    expect(pipe).toBeTruthy();
  });
});
